import React from "react";
import Login from "../LoginComponent";

/**
 * LoginPage - Wrapper that uses the redesigned professional Login component
 * Features: Institutional split-panel, theme-aware bell toggle, college system.
 */
const LoginPage: React.FC = () => {
  return <Login />;
};

export default LoginPage;
