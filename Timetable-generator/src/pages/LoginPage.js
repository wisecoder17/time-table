import React from "react";
import Login from "../Login";

/**
 * LoginPage - Wrapper that uses the redesigned professional Login component
 *
 * This page wraps the new Login component (src/Login.js) which features:
 * - Professional split-panel layout
 * - Light/Dark mode with bell icon toggle
 * - College representation system
 * - WCAG AA accessibility compliance
 * - Responsive design on all devices
 * - Institutional aesthetic for Bells University
 */
const LoginPage = () => {
  return <Login />;
};

export default LoginPage;
